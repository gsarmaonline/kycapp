// Package emailtemplates owns email template domain helpers (defaults, keys, render).
// Persistence and HTTP live in internal/store, internal/service, and internal/http.
// Kept free of KYC-specific packages so it can move to another app later.
package emailtemplates
